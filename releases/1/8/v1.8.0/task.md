# Місія релізу v1.8.0

- Міграція 12 UI моделей домену на наслідування від бази `Model` (`@nan0web/core`).
- Перетягування базової моделі для `Navigation.js`.
- Видалення ініціалізаторів instance-полів в усіх моделях `src/domain/components/`.
- Оновлення `next.md` згідно зі статусом і видалення `seed-model-migration.md`.

## Acceptance criteria (Definition of Done)

- [x] Всі моделі компонентів і `Navigation` успішно проходять тест `instanceof Model`.
- [x] `package.json` містить `@nan0web/core` у `dependencies`.
- [x] Відсутність циклічних залежностей гарантована (ui → core не циклічно).
- [x] `test:all` залишаються зеленими.

## Architecture Audit (Чекліст)

- [x] Чи прочитано Індекси екосистеми? Так, архітектурний план вже підготовлено.
- [x] Чи існують аналоги в пакетах? Ні, це безпосередня міграція базових UI-моделей.
- [x] Джерела даних: YAML, nano, md, json, csv? Ні, це системне оновлення ядра, дані не задіюються.
- [x] Чи відповідає UI-стандарту (Deep Linking)? Не стосується цього релізу.
